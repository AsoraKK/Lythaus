{
  "schemaVersion": "lythaus-github-actions-policy-v1",
  "capturedAt": "2026-08-22",
  "repository": "AsoraKK/Lythaus",
  "enabled": true,
  "allowedActions": "all",
  "shaPinnedRequired": false,
  "directActionReferencesPinned": true,
  "reason": "Repository workflows are pinned to immutable commits and CI enforces that contract. GitHub-wide SHA enforcement remains disabled because the pinned subosito/flutter-action composite currently references mutable actions/cache@v5 internally; enabling the repository setting blocks all Flutter workflows before their jobs start.",
  "mutationPerformed": true,
  "rollback": "PUT /repos/AsoraKK/Lythaus/actions/permissions with enabled:true, allowed_actions:all, sha_pinning_required:true after replacing or approving a Flutter setup action with immutable transitive references"
}
