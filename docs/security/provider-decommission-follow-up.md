# Provider decommission follow-up

This checklist separates repository decommissioning from live provider changes.
Repository automation may capture sanitized read-only provider evidence, but no
live provider resource is considered safe to delete until its current bindings,
traffic, rollback path, and replacement state have been verified.

| Surface | Current state and evidence | Desired Lythaus state | Exact proposed mutation | Rollback | Approval | Status |
| --- | --- | --- | --- | --- | --- | --- |
| GitHub owner namespace | Repository currently remains under the founder's legacy-named personal GitHub namespace | Repository hosted under an approved Lythaus-named GitHub account or organisation, with `Lythaus` as the repository name | Transfer the repository only after GitHub Apps, Cloudflare Git integration, Dependabot, Codespaces, deploy keys, webhooks, local remotes and generated repository metadata are inventoried and a transfer rollback/contact path is recorded | GitHub transfer recovery or transfer back to the prior owner if permitted; retain exact pre-transfer remote metadata | Repository owner | Pending external GitHub administration |
| Former-brand Pages project | Historical evidence records a legacy Pages project serving `admin.lythaus.co`; the fresh Cloudflare account audit is now the authority for current state | Only Lythaus-named Pages projects serve active Lythaus domains | Use the fresh read-only Cloudflare inventory, verify the replacement admin project and Access mapping, rebind `admin.lythaus.co`, run production smoke/DNS/Access checks, then retire the legacy project only after it receives no required traffic | Restore the captured prior Pages binding, DNS records, Access mapping and deployment | Cloudflare owner and release owner | Pending fresh live audit/cutover |
| Email delivery | Native Worker configuration uses the Cloudflare email binding, but provider onboarding and delivery acceptance are not yet proven | Verified Lythaus sending domain, DNS authentication, delivery telemetry, and protected secrets | Onboard the approved Lythaus sending domain, publish required DNS records, verify delivery, and enable the production binding | Disable the new sender and restore the previously approved delivery mode without exposing credentials | Cloudflare/email owner | Pending |
| PlanetScale production | Canonical repository migrations now run through `0013_marketing_waitlist.sql`; previous relation-count snapshots are not treated as current live evidence | Production `lythaus-core/main` reconciled exactly to the approved immutable migration chain, with no unknown production branch | Run the sanitized PlanetScale account audit, then the existing read-only production schema verifier; apply only reviewed forward migrations through the gated production migration workflow if live state is behind | Use forward corrective migrations or the documented restore procedure; never rewrite applied migration checksums | PlanetScale/data owner | Pending fresh live audit/reconciliation |
| PlanetScale development branch | Repository policy treats `development` as synthetic validation only and deletion-eligible after dependency proof | No permanent hosted development branch unless it has an explicit current requirement | Delete only after fresh PlanetScale inventory, Hyperdrive inspection, CI/config search, synthetic-data proof and production-schema verification establish that no live dependency remains | Recreate only from an approved source if a later validation need is documented; never use production data for recreation | PlanetScale/data owner | Pending dependency proof |
| Retired GitHub credentials | Retired-provider counters are verified zero in tracked evidence; several non-retired names still require consumer review | Zero retired credentials and purpose-documented retained credentials across repository, environment, organisation, Codespaces and Dependabot scopes | Delete only names proven unused; rotate retained Cloudflare/PlanetScale credentials after provider cleanup; separately review deploy keys, webhooks and GitHub Apps | Restore access through provider-controlled rotation only; never recover secret values from Git history | Repository owner | Pending administration review |
| Store submission | Store and signing checklist still contains owner-controlled evidence items | Verified Android/iOS metadata, signing, privacy, and release evidence | Complete provider-console evidence and release gates; no repository secret values or screenshots | Withdraw staged release and restore prior release metadata | Product/release owner | Pending |

## Repository boundary

The repository may remove obsolete code, workflows, branches and active legacy
metadata and may run sanitized provider audits. Destructive Cloudflare or
PlanetScale operations must still use current live evidence and a tested rollback
path. Historical Git commits are not rewritten solely to erase retired branding.
